#!/bin/bash

# sets up some global variables in podpatcher.py which need to be specific to the host environment
# not currently used. for now, just manually set vars like registry and base tag in podpatcher.py if needed
#TODO
